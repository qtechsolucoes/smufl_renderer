# Run with `dart run ffigen --config ffigen.yaml`.
name: VerovioFlutterBindings
description: |
  Bindings for `src/verovio_flutter.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/verovio_flutter_bindings_generated.dart'
headers:
  entry-points:
    - 'src/verovio_flutter.h'
  include-directives:
    - 'src/verovio_flutter.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
